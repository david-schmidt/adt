1. Make sure the version number is updated.  Places where 
   the version number is encoded:
   * build/build.xml: <property name="dosVersionString" value="v.rm" />
   * src/client/adt.s: DOS version number, if necessary: version_no	"v.rm"
   * doc/readme.txt: "What's new" section

2. Perform a build
   * Ensure you are using the 'all' target.

3. Commit all changes