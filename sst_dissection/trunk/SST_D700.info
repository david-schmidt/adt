GLOBAL {
    PAGELENGTH  0;
};

RANGE { START $1D00; END $1D89; Type Code; };
RANGE { START $1D8A; END $1DC4; Type BYTETABLE; };
RANGE { START $1DC5; END $1FA7; TYPE Code; };
RANGE { START $1FA8; END $1FAC; TYPE BYTETABLE; };
RANGE { START $1FAD; END $2020; TYPE Code; };
RANGE { START $2021; END $215E; TYPE BYTETABLE; };
RANGE { START $215F; END $217D; TYPE Code; };
RANGE { START $217E; END $21A3; TYPE BYTETABLE; };
RANGE { START $21A4; END $229E; TYPE Code; };

LABEL { NAME "ZPSL16";  ADDR $10; COMMENT "Slot * 16"; };
LABEL { NAME "ZPDRV";   ADDR $11; };
LABEL { NAME "ZPTRK";   ADDR $12; };

LABEL { NAME "CLBOT";           ADDR $1610; };
LABEL { NAME "XCHANGE";         ADDR $1D00; };
LABEL { NAME "PUP";             ADDR $1D16; };
LABEL { NAME "UNPACK";          ADDR $1D26; };
LABEL { NAME "UPCK1";           ADDR $1D29; };
LABEL { NAME "SETBIT";          ADDR $1D34; };
LABEL { NAME "STRBIT";          ADDR $1D3A; };
LABEL { NAME "CLMID";           ADDR $1D4A; };
LABEL { NAME "CHKDSK";          ADDR $1D52; };
LABEL { NAME "CHK1";            ADDR $1D54; };
LABEL { NAME "BAD";             ADDR $1D65; };
LABEL { NAME "BAD1";            ADDR $1D73; };
LABEL { NAME "BAD2";            ADDR $1D81; };
LABEL { NAME "WDSK";            ADDR $1D8A; };
LABEL { NAME "SIDEA";           ADDR $1DB0; };
LABEL { NAME "GOOD";            ADDR $1DC5; };
LABEL { NAME "RWTS";            ADDR $1DC8; };
LABEL { NAME "RWTS1";           ADDR $1DDE; };
LABEL { NAME "RWTS0";           ADDR $1DE3; };
LABEL { NAME "RWTS2";           ADDR $1DEC; };
LABEL { NAME "RWTS3";           ADDR $1DFD; };
LABEL { NAME "READINT";         ADDR $1DFF; };
LABEL { NAME "WRITINT";         ADDR $1E09; };
LABEL { NAME "MYREAD";          ADDR $1E13; };
LABEL { NAME "MYWRITE";         ADDR $1E34; };
LABEL { NAME "RWPROG";          ADDR $1E55; };
LABEL { NAME "SPARM";           ADDR $1E8C; };
LABEL { NAME "RILBEG";          ADDR $1E95; };
LABEL { NAME "RETRY";           ADDR $1EA7; };
LABEL { NAME "T34";             ADDR $1EC5; };
LABEL { NAME "RWLOOP";          ADDR $1ECA; };
LABEL { NAME "RWDONE";          ADDR $1EE7; };
LABEL { NAME "REB";             ADDR $1F0A; };
LABEL { NAME "RETIT";           ADDR $1F0D; };
LABEL { NAME "DONE";            ADDR $1F30; };
LABEL { NAME "DONER";           ADDR $1F33; };
LABEL { NAME "DONE2";           ADDR $1F4E; };
LABEL { NAME "RPARM";           ADDR $1F9E; };
LABEL { NAME "RILDON";          ADDR $1FA7; };
LABEL { NAME "DSKCMP";          ADDR $1FA8; };
LABEL { NAME "CHOOSE";          ADDR $1FAD; };
LABEL { NAME "CH1";             ADDR $1FBD; };
LABEL { NAME "CH2";             ADDR $1FCB; };
LABEL { NAME "CHH";             ADDR $1FD5; };
LABEL { NAME "CH3";             ADDR $1FEF; };
LABEL { NAME "CH4";             ADDR $1FFD; };
LABEL { NAME "CHI";             ADDR $2007; };
LABEL { NAME "CH5";             ADDR $2018; };
LABEL { NAME "CH6";             ADDR $201D; };
LABEL { NAME "CHTXT";           ADDR $2021; };
LABEL { NAME "PARMTXT";         ADDR $2088; };
LABEL { NAME "INITEXT";         ADDR $20CB; };
LABEL { NAME "PAK";             ADDR $20F0; };
LABEL { NAME "CLR";             ADDR $210F; };
LABEL { NAME "TDO";             ADDR $215F; };
LABEL { NAME "TDO1";            ADDR $2164; };
LABEL { NAME "TDO2";            ADDR $2172; };
LABEL { NAME "TDO3";            ADDR $2175; };
LABEL { NAME "TDOTXT";          ADDR $217E; };
LABEL { NAME "INIT";            ADDR $21A4; };
LABEL { NAME "PRINT";           ADDR $21B2; };
LABEL { NAME "INIT2";           ADDR $21C0; };
LABEL { NAME "INORG";           ADDR $21D3; };
LABEL { NAME "INDUP";           ADDR $21E6; };
LABEL { NAME "INITIT";          ADDR $21F6; };
LABEL { NAME "INITIT1";         ADDR $2201; };
LABEL { NAME "INITIT2";         ADDR $220F; };
LABEL { NAME "SETCRS";          ADDR $222B; };
LABEL { NAME "PACK";            ADDR $2236; };
LABEL { NAME "PCK1";            ADDR $2239; };
LABEL { NAME "T35";             ADDR $2254; };
LABEL { NAME "T35LP";           ADDR $2266; };
LABEL { NAME "T35LP1";          ADDR $226C; };



LABEL { NAME "CH";      ADDR $B500;
        COMMENT "Called by Pirate a Disk main menu"; };
LABEL { NAME "IN";      ADDR $B50D; };
LABEL { NAME "R";       ADDR $B517; COMMENT "Hijacked read routine"; };
LABEL { NAME "W";       ADDR $B51D; COMMENT "Hijacked write routine"; };
LABEL { NAME "LEAVE";   ADDR $B523; };
LABEL { NAME "MOVE";    ADDR $B527;
        COMMENT "Swaps RAM and banked-switch RAM"; };
LABEL { NAME "XCH1";    ADDR $B539; };
LABEL { NAME "XCH2";    ADDR $B541; };
LABEL { NAME "MREAD";   ADDR $B560; };
LABEL { NAME "MWRITE";  ADDR $B566; };

LABEL { NAME "VAR1";            ADDR $B56C; };
LABEL { NAME "VAR2";            ADDR $B56D; };
LABEL { NAME "VAR3";            ADDR $B56E; };
LABEL { NAME "FLAG";            ADDR $B56F; };
LABEL { NAME "ORSLT1";          ADDR $B570; };
LABEL { NAME "DPSLT1";          ADDR $B571; };
LABEL { NAME "ORDRV1";          ADDR $B572; };
LABEL { NAME "DPDRV1";          ADDR $B573; };
LABEL { NAME "DCOUNT1";         ADDR $B574; };
LABEL { NAME "TBEG1";           ADDR $B575; };
LABEL { NAME "TEND1";           ADDR $B576; };
LABEL { NAME "TSTEP1";          ADDR $B577; };
LABEL { NAME "NIBFLG1";         ADDR $B578; };
LABEL { NAME "SYNFLG1";         ADDR $B579; };
LABEL { NAME "SIDE";            ADDR $B57A; };
LABEL { NAME "TRK1";            ADDR $B57B; };
LABEL { NAME "NUMBER";          ADDR $B57C; };

LABEL { NAME "SLT";             ADDR $B7E9; COMMENT "RWTS slot * 16"; };
LABEL { NAME "DRV";             ADDR $B7EA; COMMENT "RWTS drive"; };
LABEL { NAME "VOL";             ADDR $B7EB; COMMENT "RWTS volume"; };
LABEL { NAME "TRK";             ADDR $B7EC; COMMENT "RWTS track"; };
LABEL { NAME "SECX";             ADDR $B7ED; COMMENT "RWTS sector"; };
LABEL { NAME "BUFLO";           ADDR $B7F0; COMMENT "RWTS buffer, low"; };
LABEL { NAME "BUFHI";           ADDR $B7F1; COMMENT "RWTS buffer, high"; };
LABEL { NAME "RWTS_BYTE_COUNT"; ADDR $B7F3; };
LABEL { NAME "CMD";             ADDR $B7F4;
        COMMENT "RWTS command: 0=SEEK, 1=READ, 2=WRITE, 4=FORMAT."; };

LABEL { NAME "ORSLT";   ADDR $B280; COMMENT "Original slot"; };
LABEL { NAME "ORDRV";   ADDR $B281; Comment "Original drive"; };
LABEL { NAME "DPSLT";   ADDR $B282; COMMENT "Duplicate slot"; };
LABEL { NAME "DPDRV";   ADDR $B283; COMMENT "Duplicate drive"; };
LABEL { NAME "XXSLT";   ADDR $B284; COMMENT "Some slot number"; };
LABEL { NAME "DCOUNT";  ADDR $B285; COMMENT "Disk count"; };
LABEL { NAME "ORTRK";   ADDR $B286; COMMENT "Initialized to $FF?"; };
LABEL { NAME "DPTRK";   ADDR $B287; };
LABEL { NAME "TBEG";    ADDR $B288; };
LABEL { NAME "TEND";    ADDR $B289; };
LABEL { NAME "TSTEP";   ADDR $B28A; };
LABEL { NAME "CURTRK";  ADDR $B28B; };
LABEL { NAME "SYNFLG";  ADDR $B28C; };
LABEL { NAME "NIBFLG";  ADDR $B28D; };

LABEL { NAME "DOS_RWTS";        ADDR $BD00; };

LABEL { NAME "KBD";     ADDR $C000; };
LABEL { NAME "KBDSTRB"; ADDR $C010; };
LABEL { NAME "TAPEOUT"; ADDR $C020; };
LABEL { NAME "SPKR";    ADDR $C030; };
LABEL { NAME "TXTCLR";  ADDR $C050; };
LABEL { NAME "TXTSET";  ADDR $C051; };
LABEL { NAME "MIXCLR";  ADDR $C052; };
LABEL { NAME "MIXSET";  ADDR $C053; };
LABEL { NAME "LOWSCR";  ADDR $C054; };

#LABEL { NAME "READBSR2";    ADDR $C080; };
#LABEL { NAME "WRITEBSR2";   ADDR $C081; };
#LABEL { NAME "OFFBSR2";     ADDR $C082; };
#LABEL { NAME "RDWRBSR2";    ADDR $C083; };
#LABEL { NAME "READBSR1";    ADDR $C088; };
#LABEL { NAME "WRITEBSR2";   ADDR $C089; };
#LABEL { NAME "OFFBSR1";     ADDR $C08A; };
#LABEL { NAME "RDWRBSR1";    ADDR $C08B; };
#LABEL { NAME "BSRBANK2";    ADDR $C011; };
#LABEL { NAME "BSRREADRAM";  ADDR $C012; };

LABEL { NAME "VTAB";    ADDR $FC22; };
LABEL { NAME "HOME";    ADDR $FC58; };
LABEL { NAME "COUT";    ADDR $FDED; };
LABEL { NAME "SETKBD";  ADDR $FE89; };
LABEL { NAME "SETVID";  ADDR $FE93; };
LABEL { NAME "OLDRST";  ADDR $FF59; };
